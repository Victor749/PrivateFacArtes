# Puerto HTTP para la app NodeJS (3000 por defecto)
PORT='3000'
# Niveles de la utilidad DEBUG para mostrar dichos log en consola
DEBUG=''
# Modo de despliegue de la app NodeJS (development por defecto)
NODE_ENV='production'
# Credenciales para conectar con servidor MySQL
LOCAL_MYSQL_HOST='localhost'
LOCAL_MYSQL_PORT='3306'
LOCAL_MYSQL_USER='root'
LOCAL_MYSQL_PASSWORD='1234'
LOCAL_MYSQL_DB='museovirtual'
# Cadena secreta para generar sesiones
SESSION_SECRET='el guardian de troya'
#### URL del Sitio Desplegado (Se usa para Login con Google)
# http://localhost:[PORT] para prueba local
# https://[DOMINIO] para la app desplegada bajo un proxy seguro
# Se debe incluir esta direccion en la configuracion de la API OAuth2 de Google (Proyecto Editor Museo Virtual Google Cloud Console)
SITE_URL='http://localhost:3000'
#### Indica si la app esta bajo un proxy seguro (HTTPS TLS/SSL) o no (yes o no) para el uso de cookies seguras
# Usar no para development y yes para production (solo si esta bajo un proxy seguro)
# Si el ambiente esta en production sin un proxy seguro y se define esta variable como yes, no se podran crear sesiones para el editor
# Ver app.js para mas informacion
COOKIES_SEGURAS='no'
# Credenciales para Login OAuth 2.0 al Editor de Google (Proyecto Editor Museo Virtual Google Cloud Console)
GOOGLE_CLIENT_ID='161525782471-43khneealveoa5vdqiv9b5pc3ofppod9.apps.googleusercontent.com'
GOOGLE_PROJECT_ID='editor-museo-virtual'
GOOGLE_CLIENT_SECRET='yyivieCe3CXUzlf-c0183Qbn'
